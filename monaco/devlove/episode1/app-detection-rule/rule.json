{
 "name": "{{ .name }}",
 "applicationIdentifier": "{{ .applicationId }}",
 "filterConfig": {
  "applicationMatchTarget": "{{ .target }}",
  "applicationMatchType": "{{ .type }}",
  "pattern": "{{ .pattern }}"
 }
}